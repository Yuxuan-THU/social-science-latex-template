% =============================================================================
%  text/RESULTS/robustness.tex
%  Robustness checks: demonstrates a subfigure environment.
%  稳健性检验示例：演示子图环境。
% =============================================================================

We subject the main results to a battery of robustness checks. First, we
re-estimate all specifications with alternative centrality measures (degree
centrality and eigenvector centrality) and with alternative network
definitions. Second, we include additional controls for organizational
background and regional characteristics. Third, we address potential
measurement error in the outcome variables using alternative coding schemes.

% 中文对照（可选）
% 稳健性检验包括：更换中心性测度与网络定义、加入更多控制变量、更换结果
% 变量编码方式等。全部结果均保持稳健（见附录）。

The main results are robust to all of these modifications. Figure~\ref{fig:robustness}
illustrates the stability of the estimated coefficients across specifications.

% 图 3：稳健性检验示意图（子图环境示例）
\begin{figure}[htbp]
  \centering
  \begin{subfigure}[b]{0.48\textwidth}
    \centering
    \includegraphics[width=\textwidth]{example-figure.pdf}
    \caption{Promotion outcome}
    \label{fig:robust-promotion}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.48\textwidth}
    \centering
    \includegraphics[width=\textwidth]{example-figure.pdf}
    \caption{Purge outcome}
    \label{fig:robust-purge}
  \end{subfigure}
  \caption{Coefficient Stability Across Robustness Specifications (Placeholder)}
  \label{fig:robustness}
\end{figure}
