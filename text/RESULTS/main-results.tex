% =============================================================================
%  text/RESULTS/main-results.tex
%  Demonstrates referencing a regression table and a figure.
%  Content is placeholder -- replace entirely.
%  演示引用回归表与图片；内容为占位，请整体替换。
% =============================================================================

This subsection demonstrates how to present and discuss the main results.
Table~\ref{tab:main-1} reports estimates of equation~\eqref{eq:regression-1};
Figure~\ref{fig:example-1} visualizes the corresponding quantities. All
numbers in the table are placeholders.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{example-figure.pdf}
  \caption{An Example Visualization (Placeholder)}
  \label{fig:example-1}
\end{figure}

% 中文对照（可选）
% 本小节演示主要结果的呈现方式：先引用回归表（表 B1），再配以图形
% （图 2）。表中数字均为占位，请替换为你自己的估计结果。
When writing this section, (i) restate the specification, (ii) describe the
sign, magnitude, and statistical significance of the coefficients, and
(iii) connect the estimates back to the hypotheses or propositions stated in
the argument section.
