% =============================================================================
%  text/HISTORICAL BACKGROUND/developments.tex
%  Demonstrates how to include a figure produced from a standalone TikZ
%  source. Content is generic placeholder text.
%  演示如何插入由独立 TikZ 源文件编译的图片；内容为通用占位。
% =============================================================================

This subsection demonstrates the figure workflow used in this template. The
figure below was produced from the standalone TikZ source
\texttt{figures/example-figure/example-figure.tex}, compiled to PDF, and
included here with \texttt{\textbackslash includegraphics}. The drawing
itself is a generic example (a stylized two-series line chart) and is
unrelated to any research topic.

% 图 1：示例折线图（TikZ 独立编译后插入）
\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.85\textwidth]{example-figure/example-figure.pdf}
  \caption{A Generic Example Figure (Placeholder)}
  \label{fig:example-timeline}
\end{figure}

% 中文对照（可选）
% 本小节演示本模板的图片工作流：先用独立 TikZ 源文件绘制图形并编译为
% PDF，再用 includegraphics 插入正文。图中内容为通用示例，请替换。
This is the end of the placeholder developments subsection.
