% =============================================================================
%  text/RESEARCH DESIGN/outcome-2.tex
%  Demonstrates a second generic regression specification. Content is
%  placeholder.
%  演示第二个通用回归设定；内容为占位，请替换。
% =============================================================================

This subsection demonstrates a second specification, using the same
notation as equation~\eqref{eq:regression-1}:

\begin{equation}
z_i = \alpha + \beta \, x_i + \mu_j + \mathbf{Z}_i'
\boldsymbol{\gamma} + \varepsilon_i ,
\label{eq:regression-2}
\end{equation}

where $z_i$ is a second outcome for unit $i$ and all other terms are defined
as before. This generic example shows how to present several related
specifications consistently.

% 中文对照（可选）
% 式 (5) 是第二个通用回归设定示例，记号与式 (4) 一致。
This is the end of the second placeholder outcome subsection.
